[www]
user = nobody
group = nobody
listen = 127.0.0.1:9000

; static pool sized to match bench concurrency=20
pm = static
pm.max_children = 20

; pass container env vars through to PHP
clear_env = no
