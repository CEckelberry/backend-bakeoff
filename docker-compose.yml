version: '3.8'

services:
  # Database
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: bakeoff
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 5s
      retries: 10
    networks:
      - backend-network

  # Database initialization
  db-init:
    image: python:3-alpine
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:password@db:5432/bakeoff
    volumes:
      - .:/app
    working_dir: /app
    command: >
      sh -c "apk add --no-cache bash postgresql-client jq &&
             bash packages/seed-data/seed.sh"
    networks:
      - backend-network

  # Backend Placeholders
  bo-go:
    build: ./apps/backends/go
    ports:
      - "8081:8080"
    environment:
      DATABASE_URL: postgresql://postgres:password@db:5432/bakeoff?search_path=bakeoff_go
      TAX_SERVICE_URL: http://tax-service:8080
      RUNTIME_NAME: go
      LOG_LEVEL: info
    depends_on:
      db-init:
        condition: service_completed_successfully
    networks:
      - backend-network

  bo-rust:
    build: ./apps/backends/rust
    ports:
      - "8082:8080"
    environment:
      DATABASE_URL: postgresql://postgres:password@db:5432/bakeoff?search_path=bakeoff_rust
      TAX_SERVICE_URL: http://tax-service:8080
      RUNTIME_NAME: rust
      LOG_LEVEL: info
    depends_on:
      db-init:
        condition: service_completed_successfully
    networks:
      - backend-network

  bo-bun:
    build: ./apps/backends/bun
    ports:
      - "8083:8080"
    environment:
      DATABASE_URL: postgresql://postgres:password@db:5432/bakeoff?search_path=bakeoff_bun
      TAX_SERVICE_URL: http://tax-service:8080
      RUNTIME_NAME: bun
      LOG_LEVEL: info
      LISTEN_ADDR: 0.0.0.0
      LISTEN_PORT: 8080
    depends_on:
      db-init:
        condition: service_completed_successfully
    networks:
      - backend-network

  bo-node:
    build: ./apps/backends/node
    ports:
      - "8084:8080"
    environment:
      DATABASE_URL: postgresql://postgres:password@db:5432/bakeoff?search_path=bakeoff_node
      TAX_SERVICE_URL: http://tax-service:8080
      RUNTIME_NAME: node
      LOG_LEVEL: info
      LISTEN_ADDR: 0.0.0.0
      LISTEN_PORT: 8080
    depends_on:
      db-init:
        condition: service_completed_successfully
    networks:
      - backend-network

  bo-python:
    build: ./apps/backends/python
    ports:
      - "8085:8080"
    environment:
      DATABASE_URL: postgresql://postgres:password@db:5432/bakeoff?search_path=bakeoff_python
      TAX_SERVICE_URL: http://tax-service:8080
      RUNTIME_NAME: python
      LOG_LEVEL: info
      LISTEN_ADDR: 0.0.0.0
      LISTEN_PORT: 8080
    depends_on:
      db-init:
        condition: service_completed_successfully
    networks:
      - backend-network

  bo-php:
    image: python:3-alpine
    ports:
      - "8086:8080"
    command: python -m http.server 8080
    
    networks:
      - backend-network

  # Tax Service
  tax-service:
    build: ./apps/tax-service
    ports:
      - "8087:8080"
    networks:
      - backend-network

  # Router
  router:
    image: python:3-alpine
    ports:
      - "8090:8080"
    command: python -m http.server 8080
    
    networks:
      - backend-network

  # Prometheus
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9092:9090"
    volumes:
      - ./infra/observability/prometheus-local.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
    networks:
      - backend-network

  # Grafana
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_SECURITY_ADMIN_USER: admin
    volumes:
      - grafana_data:/var/lib/grafana
    networks:
      - backend-network

volumes:
  postgres_data:
  prometheus_data:
  grafana_data:

networks:
  backend-network:
    driver: bridge
