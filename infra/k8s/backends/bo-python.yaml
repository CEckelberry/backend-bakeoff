apiVersion: apps/v1
kind: Deployment
metadata:
  name: bo-python
  namespace: bakeoff
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: bo-python
  template:
    metadata:
      labels:
        app: bo-python
        runtime: python
    spec:
      serviceAccountName: bakeoff-workload
      initContainers:
        - name: cloud-sql-proxy
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2
          restartPolicy: Always
          args:
            - "--structured-logs"
            - "--port=5432"
            - "exalted-kit-496514-s1:us-west1:portfolio-db"
          securityContext:
            runAsNonRoot: true
          resources:
            requests:
              cpu: "25m"
              memory: "32Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
        - name: wait-for-proxy
          image: busybox
          command: ['sh', '-c', 'until nc -z 127.0.0.1 5432; do sleep 1; done']
      containers:
        - name: bo-python
          image: us-west1-docker.pkg.dev/exalted-kit-496514-s1/portfolio/bakeoff-python:2043ba2a
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: bakeoff-db
                  key: password
            - name: DATABASE_URL
              value: "postgresql://bakeoff:$(DB_PASSWORD)@127.0.0.1:5432/bakeoff?search_path=bakeoff_python"
            - name: RUNTIME_NAME
              value: "python"
            - name: TAX_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: bakeoff-config
                  key: TAX_SERVICE_URL
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: bakeoff-config
                  key: LOG_LEVEL
            - name: LISTEN_ADDR
              valueFrom:
                configMapKeyRef:
                  name: bakeoff-config
                  key: LISTEN_ADDR
            - name: LISTEN_PORT
              valueFrom:
                configMapKeyRef:
                  name: bakeoff-config
                  key: LISTEN_PORT
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: bo-python
  namespace: bakeoff
spec:
  selector:
    app: bo-python
  ports:
    - port: 8080
      targetPort: 8080
